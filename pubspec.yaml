name: sport_activity_dashboard_app

version: 1.0.0+1

environment:
  sdk: ">=2.2.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  cupertino_icons: ^0.1.2
  response:
  font_awesome_flutter:
  after_layout:
  google_fonts:
  provider:
  flutter_sequence_animation: ^3.0.1
  flutter_animation_progress_bar:
  flare_flutter:

    
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/chart_animation_v4.flr
    - google_fonts/

  # example:
  # fonts:
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
